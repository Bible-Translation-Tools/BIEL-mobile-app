# Flatten node_modules for React Native native builds on Windows.
node-linker=hoisted
# Avoid junction/symlink targets under .pnpm — Gradle resolves real paths and
# hits CMake's 250-char object path limit.
symlink=false
